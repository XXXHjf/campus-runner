# Copy to /root/campus-runner.env on the production server.
# Keep the real file readable by root only (chmod 600).
DB_PASSWORD=replace-with-production-database-password
# Leave false by default. Enable only when explicitly requested for development testing.
MOCK_PAYMENT_ENABLED=false
# Optional if Java is installed elsewhere:
# JAVA_BIN=/path/to/java
