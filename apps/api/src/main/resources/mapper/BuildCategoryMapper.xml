<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mikasa.campusrunner.mapper.BuildCategoryMapper">

    <select id="getIdByNumberId" resultType="java.lang.Long">
        select id from tb_build_category
        where number_id = #{numberId} and deleted = 0
    </select>

    <select id="getThree" resultType="com.mikasa.campusrunner.pojo.vo.AddressBookThreeVO$BuildCategory">
        select
            name AS build_category_name,
            number_id
        from tb_build_category
        where deleted = 0 AND school_id = #{schoolId}
    </select>

    <select id="getIdBySchoolIdAndCompusIdAndName" resultType="java.lang.Long">
        select id
        from tb_build_category
        where school_id = #{schoolId} AND
              compus_id = #{compusId} AND
              name = #{name} AND
              deleted = 0

    </select>

    <select id="getById" resultType="com.mikasa.campusrunner.pojo.entity.BuildCategory">
        select
            id,
            school_id,
            compus_id,
            name,
            number_id,
            deleted
        from tb_build_category
        where id = #{id} AND
              deleted = 0
    </select>
    <select id="getMaxNumberId" resultType="java.lang.Long">
        select number_id
        from tb_build_category
        where school_id = #{schoolId} AND
              compus_id = #{compusId} AND
              deleted = 0
        order by number_id desc
        limit 1
    </select>


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tb_build_category
        values (
                null,
                #{schoolId},
                #{compusId},
                #{name},
                #{numberId},
                #{deleted}
               )
    </insert>

</mapper>