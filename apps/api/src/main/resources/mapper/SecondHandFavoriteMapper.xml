<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mikasa.campusrunner.mapper.SecondHandFavoriteMapper">
    <insert id="insertIgnore">
        insert ignore into tb_second_hand_favorite(user_id, product_id, create_time)
        values(#{userId}, #{productId}, #{createTime})
    </insert>

    <delete id="delete">
        delete from tb_second_hand_favorite
        where user_id = #{userId} and product_id = #{productId}
    </delete>

</mapper>
