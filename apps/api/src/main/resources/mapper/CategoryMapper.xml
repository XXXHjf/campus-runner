<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mikasa.campusrunner.mapper.CategoryMapper">


    <select id="getById" resultType="com.mikasa.campusrunner.pojo.entity.Category">
        select * from tb_category where id = #{id} and deleted = 0
    </select>

    <select id="getByIdForUpdate" resultType="com.mikasa.campusrunner.pojo.entity.Category">
        select * from tb_category where id = #{id} and deleted = 0 for update
    </select>

    <select id="getAll" resultType="com.mikasa.campusrunner.pojo.entity.Category">
        select * from tb_category where deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into tb_category (category_name, image, deleted)
        values (#{categoryName}, #{image}, 0)
    </insert>

    <update id="update">
        update tb_category
        <set>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="image != null and image != ''">image = #{image},</if>
        </set>
        where id = #{id} and deleted = 0
    </update>

    <update id="deleteById">
        update tb_category set deleted = 1 where id = #{id}
    </update>
</mapper>
