@import "../../../../styles/second-hand-card.wxss";

page {
  background: var(--cr-color-background);
}

.take-orders {
  min-height: 100vh;
  overflow-x: hidden;
}

.take-orders__controls {
  position: fixed;
  top: 0;
  right: 0;
  left: 0;
  z-index: 10;
  background: var(--cr-color-surface);
}

.take-orders__nav {
  box-sizing: border-box;
  display: flex;
  align-items: center;
  gap: 10rpx;
  min-height: 44px;
  padding-left: 24rpx;
}

.take-orders__back {
  display: flex;
  flex: none;
  align-items: center;
  justify-content: center;
  width: 62rpx;
  height: 72rpx;
}

.take-orders__search {
  flex: 1;
  min-width: 0;
  --td-search-height: 70rpx;
  --td-search-padding: 0 20rpx;
  --td-search-font-size: 26rpx;
  --td-search-bg-color: var(--cr-color-background);
}

.take-orders__tabs {
  box-sizing: border-box;
  display: flex;
  width: 100%;
  height: 84rpx;
  padding: 0 8rpx;
  border-bottom: 1rpx solid var(--cr-color-border);
}

.take-orders__tab {
  position: relative;
  box-sizing: border-box;
  display: flex;
  flex: 1;
  min-width: 0;
  height: 83rpx;
  align-items: center;
  justify-content: center;
  color: var(--cr-color-text-secondary);
  font-size: 23rpx;
  white-space: nowrap;
}

.take-orders__tab--active {
  color: var(--cr-color-primary);
  font-weight: 700;
}

.take-orders__tab--active::after {
  position: absolute;
  right: 35%;
  bottom: 0;
  left: 35%;
  height: 5rpx;
  border-radius: 5rpx;
  background: var(--cr-color-primary);
  content: '';
}

.take-orders__list {
  margin: 24rpx var(--cr-page-padding) 48rpx;
  overflow: hidden;
  border-radius: var(--cr-radius-large);
  background: var(--cr-color-surface);
  box-shadow: var(--cr-shadow-card);
}

.take-orders__item {
  padding: 28rpx 26rpx 30rpx;
}

.take-orders__item--separated {
  border-top: 1rpx solid var(--cr-color-border);
}

.take-orders__item-top,
.take-orders__item-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14rpx;
}

.take-orders__item-top {
  margin-bottom: 28rpx;
}

.take-orders__sender {
  display: flex;
  flex: 1;
  align-items: center;
  gap: 10rpx;
  min-width: 0;
}

.take-orders__avatar,
.take-orders__avatar-fallback {
  flex: none;
  width: 48rpx;
  height: 48rpx;
  overflow: hidden;
  border-radius: 50%;
}

.take-orders__avatar-fallback {
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--cr-color-background);
}

.take-orders__name {
  min-width: 0;
  overflow: hidden;
  color: var(--cr-color-text);
  font-size: 24rpx;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.take-orders__type,
.take-orders__status {
  flex: none;
  padding: 7rpx 14rpx;
  border-radius: var(--cr-radius-small);
  font-size: 24rpx;
  line-height: 1.3;
  white-space: nowrap;
}

.take-orders__type {
  max-width: 132rpx;
  overflow: hidden;
  color: var(--cr-color-primary);
  background: var(--cr-color-primary-light);
  text-overflow: ellipsis;
}

.take-orders__type--purchase,
.take-orders__status--pending {
  color: #dc612c;
  background: #fff2e9;
}

.take-orders__status--active {
  color: var(--cr-color-primary);
  background: var(--cr-color-primary-light);
}

.take-orders__status--done {
  color: var(--cr-color-success);
  background: #e6f8ee;
}

.take-orders__status--neutral {
  color: var(--cr-color-text-secondary);
  background: var(--cr-color-background);
}

.take-orders__status--warning {
  color: var(--cr-color-danger);
  background: #fff1f0;
}

.take-orders__main {
  display: flex;
  align-items: center;
  gap: 18rpx;
  min-width: 0;
}

.take-orders__cover {
  flex: none;
  width: 124rpx;
  height: 124rpx;
  overflow: hidden;
  border-radius: var(--cr-radius-medium);
  --td-image-round-radius: var(--cr-radius-medium);
}

.take-orders__title {
  display: block;
  min-width: 0;
  flex: 1;
  overflow: hidden;
  color: var(--cr-color-text);
  font-size: 29rpx;
  font-weight: 700;
  line-height: 1.4;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.take-orders__amount {
  flex: 0 0 160rpx;
  width: 160rpx;
  margin-left: auto;
  color: #e7552d;
  font-size: 30rpx;
  font-weight: 700;
  text-align: right;
  white-space: nowrap;
}

.take-orders__item-bottom {
  margin-top: 32rpx;
}

.take-orders__more {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 68rpx;
  height: 56rpx;
}

.take-orders__action {
  flex: none;
  margin-left: auto;
}

.take-orders__sheet {
  padding: 24rpx 32rpx 16rpx;
}

.take-orders__sheet-separator {
  height: 1rpx;
  margin: 12rpx 0;
  background: var(--cr-color-border);
}

.take-orders__feedback {
  display: flex;
  justify-content: center;
  padding: 90rpx 0;
}
