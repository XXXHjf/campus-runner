# Campus Runner 项目约束

本文件适用于整个仓库。处理构建、部署、服务器更新或生产故障时，必须先阅读
`docs/deployment/server-operations.md`，并逐项执行其中的“生产发布必检清单”。

## 生产配置

- 不得假设本地配置可以直接用于生产。尤其是生产 MySQL 密码与本地开发密码
  `admin` 不同；生产必须通过权限为 `600` 的 `/root/campus-runner.env` 注入
  `DB_PASSWORD`，不得再把生产密码直接写入打包配置。
- 不得把生产密码、AccessKey、支付私钥或其他秘密写入 Git、文档、提交信息或日志。
- `apps/api/src/main/resources/application.yaml` 被 Git 忽略，但会被 Maven 打入 JAR。
  因此每次 API 打包前都必须人工核对该文件，不能因为 `git status` 干净就跳过。
- 当前生产拓扑是 Nginx 监听 HTTPS，Spring Boot 仅监听本机 HTTP `8080`：
  `server.port=8080`、`server.ssl.enabled=false`。
- 生产环境必须关闭模拟支付，并确认支付私钥存在且与生产商户配置匹配。
- 当前 admin 生产构建使用同域 API，`VITE_API_BASE_URL` 保持为空，由 Nginx 代理
  `/api/` 和 `/admin/api/` 到 `127.0.0.1:8080`。

## 发布行为

- 部署 API 或 admin 前，先报告必检清单结果和仍未确认的项目；生产秘密只报告
  “已确认/未确认”，不得显示具体值。
- 涉及数据库结构变化时，先备份数据库并执行对应迁移，再启动依赖新结构的 API。
- 发布后必须验证主页、登录接口、至少一个数据库接口、Java 进程、`8080` 端口和
  后端日志。日志中出现 MySQL `1045 Access denied` 时，优先检查打入 JAR 的数据库凭据。
- 生产 API 必须使用仓库 `apps/api/deploy/` 下的启动和停止脚本；不要恢复为直接运行
  `nohup java -jar ...`，否则可能丢失环境变量、Java 路径和生产安全覆盖项。
- 除非用户明确要求，不得仅通过延长前端超时时间掩盖后端连接或数据库故障。

## 用户可见文案

处理管理端、小程序或 API 返回给界面的文案时，必须始终从管理员或普通用户的视角
表达，不得把需求原文、开发任务说明或内部实现机制直接展示在产品界面中。

- 文案不帮助用户理解当前状态、操作后果或下一步动作时，直接删除，不为解释实现而
  增加提示。
- 确有必要保留时，只说明“发生了什么、会有什么影响、用户应该怎么做”。不得展示
  “临时资源、正式关联、自动释放、服务端定时清理、后端、接口、字段、数据库、
  token、路由、方法名”等实现术语，也不得展示调试建议、英文错误码或开发进度清单。
- 技术细节应写入代码注释、日志或对应技术文档；服务端可以记录详细异常，但返回给
  界面的错误消息必须简洁、可理解、可操作，并避免泄露内部结构。
- 新增或修改一个页面、弹窗、表单、空状态、确认框、Toast 或错误处理时，不只检查
  当前一句文案，还要排查同一流程和相邻功能是否存在同类开发者视角文案；必要时同步
  整改管理端、小程序和 API 返回消息。
- 交付前应搜索本次影响范围内的用户可见字符串，重点检查 TSX/JSX、WXML、页面 JS
  中的提示，以及 API 的 `Result.error` 和业务异常消息。代码注释和仅写入开发者
  控制台的日志不属于界面文案，但不得因此遗漏会被请求层透传到界面的异常内容。

改写示例：

- 删除：“图片保存时才会正式关联，取消后自动释放”。
- 改为：“图片上传成功，请点击‘保存’完成新增”，而不是“图片已暂存，保存后正式
  关联”。
- 改为：“取消后商品将重新上架”，而不是“取消后商品会释放回市场”。
- 改为：“登录已失效，请重新登录”，而不是“NO_TOKEN: Missing token in request”。

## 管理端组件与样式复用

管理端新增或修改页面时，必须优先复用现有设计系统和公共实现，避免页面之间各自手写
同类控件。当前统一基线为 Ant Design、`apps/admin/src/theme/adminTheme.ts` 主题配置，
以及 `apps/admin/src/components/admin/` 下的管理端公共页面组件。

- 页面标题、筛选栏、内容卡片、数量标识、表格空状态等通用结构必须优先使用
  `AdminPage`、`AdminPageHeader`、`AdminFilterBar`、`AdminContentCard`、
  `AdminCount` 和 `createAdminTableLocale`，不得在新页面复制一套近似结构。
- 输入框、选择器、按钮、表格、分页、表单、弹窗、抽屉、提示和上传等交互，Ant Design
  已提供满足需求的组件时不得重新使用原生 HTML 和页面级 CSS 手工实现。确有业务差异时
  优先扩展公共组件的可配置能力，而不是复制后修改。
- 同一种页面结构、交互或样式在第二处出现，或已经明确后续会被多个页面使用时，应在
  当前任务中抽到公共组件、Hook、工具函数或主题令牌中；一次性的业务字段和业务流程不应
  为了形式过度抽象。
- 页面 CSS 必须以页面根类名限定作用域；禁止在页面样式中定义 `.page-header`、
  `.toolbar`、`.search-box`、`.table-container`、`.btn-primary`、`.modal-content`
  等无作用域通用选择器。跨页面样式只能维护在明确的公共样式或主题文件中。
- 图标必须来自项目统一使用的图标库，不得使用 Emoji、文本符号或临时绘制的图形代替
  正式界面图标。空状态、加载状态和错误状态应使用统一组件并提供中文用户文案。
- 修改公共组件或主题后，必须检查所有调用页面的桌面和窄屏表现；新增页面交付前应搜索
  是否又出现了可复用结构的重复实现。

## 长期知识沉淀

- 完成修改后必须判断是否产生了以后仍有价值的知识。如果涉及稳定的架构事实、生产
  拓扑、部署步骤、配置约束、数据库迁移、外部服务接入、重复踩坑、故障根因或关键
  设计决策，应在同一次任务中主动更新最合适的仓库文档，不必等待用户再次提醒。
- 优先更新已有的权威文档，避免为同一主题创建多份相互冲突的说明。部署和生产故障
  统一维护在 `docs/deployment/`，数据库变化维护在 `docs/database/`，接口行为维护在
  `docs/api/`；跨项目的强制约束才写入本文件。
- 文档应记录“为什么、当前约束、验证方法和回滚方法”，不能只记录执行过的命令。
- 临时 PID、一次性时间戳、短期调试输出、可从代码直接看出的细枝末节以及任何密码、
  token、私钥等敏感值不进入长期文档。
- 修改完成后在最终回复中说明更新了哪些长期文档；若判断无需沉淀，也无需为了形式
  创建无价值文档。
