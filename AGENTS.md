# Campus Runner 项目约束

本文件适用于整个仓库。处理构建、部署、服务器更新或生产故障时，必须先阅读
`docs/deployment/server-operations.md`，并逐项执行其中的“生产发布必检清单”。

## 生产配置

- 不得假设本地配置可以直接用于生产。尤其是生产 MySQL 密码与本地开发密码
  `admin` 不同；生产必须通过权限为 `600` 的 `/root/campus-runner.env` 注入
  `DB_PASSWORD`，不得再把生产密码直接写入打包配置。
- 不得把生产密码、AccessKey、支付私钥或其他秘密写入 Git、文档、提交信息或日志。
- `apps/api/src/main/resources/application.yaml` 被 Git 忽略，但会被 Maven 打入 JAR。
  因此每次 API 打包前都必须人工核对该文件，不能因为 `git status` 干净就跳过。
- 当前生产拓扑是 Nginx 监听 HTTPS，Spring Boot 仅监听本机 HTTP `8080`：
  `server.port=8080`、`server.ssl.enabled=false`。
- 生产环境必须关闭模拟支付，并确认支付私钥存在且与生产商户配置匹配。
- 当前 admin 生产构建使用同域 API，`VITE_API_BASE_URL` 保持为空，由 Nginx 代理
  `/api/` 和 `/admin/api/` 到 `127.0.0.1:8080`。

## 发布行为

- 部署 API 或 admin 前，先报告必检清单结果和仍未确认的项目；生产秘密只报告
  “已确认/未确认”，不得显示具体值。
- 涉及数据库结构变化时，先备份数据库并执行对应迁移，再启动依赖新结构的 API。
- 发布后必须验证主页、登录接口、至少一个数据库接口、Java 进程、`8080` 端口和
  后端日志。日志中出现 MySQL `1045 Access denied` 时，优先检查打入 JAR 的数据库凭据。
- 生产 API 必须使用仓库 `apps/api/deploy/` 下的启动和停止脚本；不要恢复为直接运行
  `nohup java -jar ...`，否则可能丢失环境变量、Java 路径和生产安全覆盖项。
- 除非用户明确要求，不得仅通过延长前端超时时间掩盖后端连接或数据库故障。

## 长期知识沉淀

- 完成修改后必须判断是否产生了以后仍有价值的知识。如果涉及稳定的架构事实、生产
  拓扑、部署步骤、配置约束、数据库迁移、外部服务接入、重复踩坑、故障根因或关键
  设计决策，应在同一次任务中主动更新最合适的仓库文档，不必等待用户再次提醒。
- 优先更新已有的权威文档，避免为同一主题创建多份相互冲突的说明。部署和生产故障
  统一维护在 `docs/deployment/`，数据库变化维护在 `docs/database/`，接口行为维护在
  `docs/api/`；跨项目的强制约束才写入本文件。
- 文档应记录“为什么、当前约束、验证方法和回滚方法”，不能只记录执行过的命令。
- 临时 PID、一次性时间戳、短期调试输出、可从代码直接看出的细枝末节以及任何密码、
  token、私钥等敏感值不进入长期文档。
- 修改完成后在最终回复中说明更新了哪些长期文档；若判断无需沉淀，也无需为了形式
  创建无价值文档。
