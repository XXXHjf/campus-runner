# Campus Runner 项目约束

本文件适用于整个仓库。

原则优先级：

1. 生产安全与数据安全
2. 用户明确要求
3. 本文件约束
4. 项目现有架构、设计系统与代码惯例
5. 默认开发习惯

涉及构建、部署、服务器更新、数据库迁移或生产故障时，必须先阅读：

`docs/deployment/server-operations.md`

并执行其中的生产发布必检清单。

---

## 1. 生产与安全

### 基本原则

* 不得假设本地配置可直接用于生产。
* 不得将生产密码、AccessKey、支付私钥、Token 或其他秘密写入 Git、文档、提交信息、前端代码或日志。
* 生产秘密只能报告“已确认 / 未确认”，不得输出具体值。
* 涉及生产配置、数据库、支付、部署时，不得为了省事绕过既有安全机制。

### 当前生产约束

* 生产 MySQL 密码通过权限为 `600` 的 `/root/campus-runner.env` 注入 `DB_PASSWORD`。
* `apps/api/src/main/resources/application.yaml` 虽被 Git 忽略，但会被 Maven 打入 JAR；API 打包前必须核对。
* 当前拓扑：

  * Nginx 对外提供 HTTPS
  * Spring Boot 仅监听本机 HTTP `8080`
  * `server.port=8080`
  * `server.ssl.enabled=false`
* admin 生产环境使用同域 API，`VITE_API_BASE_URL` 保持为空，由 Nginx 代理 `/api/` 与 `/admin/api/`。
* 生产默认关闭模拟支付。只有用户明确要求线上开发测试时才可开启 `MOCK_PAYMENT_ENABLED=true`，且不得将模拟结果视为真实资金到账。
* API 必须使用 `apps/api/deploy/` 下现有启动、停止脚本，不得改回直接执行 `nohup java -jar ...`。

具体发布、验证、故障排查与回滚流程以：

`docs/deployment/server-operations.md`

为准，不在本文件重复维护。

---

## 2. 开发修改原则

### 最小必要修改

默认采用增量修改：

* 只修改实现当前需求所必要的代码。
* 未被需求涉及的现有页面结构、交互、样式和文案默认保持不变。
* 不因局部需求主动重构整个页面或重新设计已有功能。
* 如发现必须扩大修改范围才能正确实现，应说明原因。

### 检查范围

日常开发只执行与本次改动直接相关的最低必要检查，例如：

* 语法
* 类型
* 组件引用
* 关键逻辑
* 受影响调用方

不要默认执行全量构建、全量回归或为低风险改动新增复杂测试。

修改公共组件、公共 Hook、主题或共享逻辑时，应检查受影响调用方。

### 人工验收分工

前端视觉和交互默认由用户人工验收。

除非用户明确要求，不进行高成本的：

* 浏览器自动化
* 反复截图
* 桌面应用自动点击
* 多分辨率穷举
* 临时搭建复杂 UI 自动测试

交付时简要说明：

* 实际执行了哪些检查
* 哪些内容需要用户人工验证

不得把静态检查或模拟测试表述为已经完成真机、界面或端到端验证。

生产发布不受本节简化规则影响，仍必须执行完整生产必检。

---

## 3. 用户可见文案

### 核心原则

**需求是实现依据，不是文案素材。**

不要把用户的需求语言直接当成 UI 文案。

先理解用户场景，再判断是否需要提示；需要提示时，应从最终用户视角重新撰写，而不是直接展示或简单改写需求原文。

### 文案规则

1. 功能优先通过交互体现。

   能通过按钮状态、显示/隐藏、可编辑/禁用状态、操作反馈等方式自然表达的，不额外增加解释功能规则的文字。

2. 可以增加真正有帮助的提示。

   适用于：

   * 操作失败原因
   * 必要前置条件
   * 重要限制
   * 操作结果
   * 防止误操作

3. 文案必须简短、自然、可操作。

   描述：

   > 用户现在需要知道什么、发生了什么、接下来应该怎么做。

   不描述系统内部如何实现。

4. 用户界面不得出现开发或实现术语，例如：

   * 后端
   * 接口
   * 数据库
   * 字段
   * token
   * 路由
   * 方法名
   * 定时清理
   * 临时资源 / 正式关联
   * 调试信息
   * 英文错误码
   * 开发进度描述

5. API 返回给界面的错误信息同样遵守以上规则。详细异常可记录在服务端日志，但用户消息应简洁、可理解、可操作，并避免泄露内部结构。

例如：

* 不写：“待接单状态支持修改说明和图片”

* 应直接开放对应编辑能力

* 不写：“NO_TOKEN: Missing token in request”

* 可写：“登录已失效，请重新登录”

* 不写：“图片保存时才会正式关联，取消后自动释放”

* 可写：“图片上传成功，请点击‘保存’完成新增”

修改页面、弹窗、表单、Toast、错误处理等用户流程时，应顺带检查本次影响范围内是否存在同类开发者视角文案。

---

## 4. 管理端 UI 与组件复用

管理端以以下实现作为统一基线：

* Ant Design
* `apps/admin/src/theme/adminTheme.ts`
* `apps/admin/src/components/admin/`

### 复用原则

已有公共组件或 Ant Design 能满足需求时，优先复用，不重新手写近似实现。

常用页面结构优先使用：

* `AdminPage`
* `AdminPageHeader`
* `AdminFilterBar`
* `AdminContentCard`
* `AdminCount`
* `createAdminTableLocale`

输入框、选择器、按钮、表格、分页、表单、弹窗、抽屉、提示、上传等交互，优先使用 Ant Design。

确有稳定的共享需求时再扩展公共组件，不为一次性业务需求过度抽象。

### 样式规则

* 页面 CSS 必须由页面根类名限定作用域。
* 页面样式中不得定义无作用域的通用选择器，例如 `.page-header`、`.toolbar`、`.search-box`、`.table-container`、`.btn-primary`。
* 跨页面样式应进入公共样式或主题。
* 图标使用项目统一图标库，不用 Emoji、文本符号或临时图形代替正式 UI 图标。
* 空状态、加载状态、错误状态使用统一组件与面向用户的中文文案。

如果同类结构或样式开始重复出现，应优先考虑集中管理，但不要为了形式强行封装。

---

## 5. 数据库与生产变更

涉及数据库结构变化时：

1. 明确迁移内容和受影响代码。
2. 生产执行前备份数据库。
3. 执行迁移。
4. 再启动依赖新结构的 API。
5. 按生产操作文档完成发布后验证。

不得让依赖新结构的代码先于数据库迁移上线。

除非用户明确要求，不得通过单纯增加前端超时时间掩盖后端、网络或数据库故障。

---

## 6. 长期知识与文档

### 事实来源

修改文档前应核对当前：

* 代码
* 数据库迁移
* 配置
* 受影响调用方

运行时事实以当前代码和实际环境为准。

生产数据库结构、运行开关和生产状态应在目标环境核实，不能仅凭仓库推断。

历史设计稿、旧响应示例、一次性计划和旧文档不得覆盖当前实现。

### 文档维护

同一主题尽量只维护一个权威入口。

优先更新已有文档，不为同一主题重复创建说明。

目录约定：

* 部署与生产故障：`docs/deployment/`
* 数据库：`docs/database/`
* API 行为：`docs/api/`
* 跨项目强制约束：本文件

代码与文档冲突时：

* 能由代码确认的，修正文档。
* 无法由代码确认的产品规则、资金语义或生产事实，不得自行推断，应明确列出待确认项。

### 何时需要沉淀

完成修改后判断是否产生以后仍有价值的知识。

以下内容通常需要同步更新文档：

* 稳定架构事实
* 生产拓扑或部署方式
* 配置约束
* 数据库迁移
* 外部服务接入
* 重复踩坑
* 故障根因
* 关键设计决策

无需沉淀：

* 临时 PID
* 一次性时间戳
* 临时调试输出
* 可直接从代码看出的琐碎实现
* 密码、Token、私钥等秘密

长期文档应重点记录：

* 为什么这样做
* 当前约束
* 如何验证
* 如何回滚

如本次任务更新了长期文档，在最终回复中简要说明更新内容；如无需沉淀，不必为了形式新增文档。
